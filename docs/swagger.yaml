basePath: /api/v1
host: localhost:3000
info:
  contact: {}
  description: API Server for Travels web application
  title: Travels API
  version: "1.0"
paths:
  /example/helloworld:
    get:
      consumes:
      - application/json
      description: do ping
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
